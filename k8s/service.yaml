apiVersion: v1
kind: Service
metadata:
  name: request-tracing-lab
  namespace: request-tracing-lab
  labels:
    app.kubernetes.io/name: request-tracing-lab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: request-tracing-lab
  ports:
    - name: http
      port: 80
      targetPort: http

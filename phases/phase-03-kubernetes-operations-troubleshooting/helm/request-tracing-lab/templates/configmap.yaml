apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "request-tracing-lab.fullname" . }}-config
  labels:
    {{- include "request-tracing-lab.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

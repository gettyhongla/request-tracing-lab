# Docker

Use this directory for Docker-specific examples, build notes, and container runtime references for Phase 3.

The root `Dockerfile` remains the primary application image definition until this phase needs additional Docker assets.

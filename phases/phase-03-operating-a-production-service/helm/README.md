# Helm

Use this directory for Helm chart notes and chart files after the raw Kubernetes manifests are understood.

Helm belongs in Phase 3 because it packages Kubernetes resources for repeatable installation, upgrade, and rollback. It should reduce duplication without hiding what the rendered Kubernetes objects do.
